# test_spa_004

Tests SPA numeric-add effects.

It invokes a target action whose numeric precondition is satisfied by a prerequisite action. The prerequisite uses a `numeric-add` effect with its input constant to produce the required state.
